trigger:
- main

pool:
  vmImage: 'windows-latest'

steps:

# Validate Orchestrator Connection

- powershell: |

    Write-Host "Organization URL:"
    Write-Host "$(UIPATH_ORG_URL)"

    Write-Host "Tenant:"
    Write-Host "$(UIPATH_TENANT)"

  displayName: 'Print UiPath Details'

- task: UiPathPack@6
  inputs:
    projectJsonPath: '$(Build.SourcesDirectory)'
    versionType: 'AutoVersion'
    outputPath: '$(Build.ArtifactStagingDirectory)\Output'

- powershell: |
    Get-ChildItem "$(Build.ArtifactStagingDirectory)\Output"
  displayName: 'Verify Package'

- task: UiPathDeploy@6
  inputs:
    orchestratorConnection: 'Dev'
    packagesPath: '$(Build.ArtifactStagingDirectory)\Output'
    createProcess: false
    folderName: 'Shared'